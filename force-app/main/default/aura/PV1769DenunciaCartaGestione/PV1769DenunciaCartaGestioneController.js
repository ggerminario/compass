({
    doInit: function (component, event, helper) {
        helper.init(component, event, helper);
    },

    //metodo richiamato dal bottone conferma del contenitore PVInserimento
    save: function (component, event, helper) {
        helper.save(component, event, helper);
    },

    componiEmail: function (component, event, helper) {
        helper.componiEmail(component, event, helper);
    },

    impostaIndirizzi: function (component, event, helper) {
        helper.impostaIndirizzi(component, event, helper);
    },

    emailcomposerwithaddress: function(component, event, helper){
        helper.emailcomposerwithaddress(component, event, helper);
    }

})